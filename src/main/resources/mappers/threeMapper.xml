<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.threeMapper">
    <insert id="selectYourEntities" parameterType="com.example.demo.dto.dtoClass">
        INSERT INTO THREE_D (
               TITLE
             , BIGO
             , WIDTH
             , HEIGHT
             , VERTICAL
             , X_CHUK
             , Y_CHUK
             , Z_CHUK
        ) VALUES (
               #{title}
             , #{bigo}
             , #{width}
             , #{height}
             , #{vertical}
             , #{x_chuk}
             , #{y_chuk}
             , #{z_chuk}
        )
    </insert>

    <!-- <select id="selectTest" parameterType="HashMap" resultType="java.util.LinkedHashMap">
	 	SELECT '123' 
         FROM DUAL
	</select> -->


</mapper>